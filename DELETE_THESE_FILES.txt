After copying in the updated files from this package, DELETE these files
from your website2026 project root (they're stale duplicates of the real
src/ versions and are not used by the build):

  AdminApp.tsx
  App.tsx
  CourseDetailPage.tsx
  ExamModule.tsx
  OnboardingFeedback.tsx
  data/.encryption_key   (was accidentally committed; regenerates itself on next boot)

Everything else in this zip should be copied into your project at the
matching relative path, overwriting the existing file.
